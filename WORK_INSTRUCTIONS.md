# UG Kitting Tool 作業手順

## キッティングフロー

UG65 は出荷時に固定 IP `192.168.23.150` を使用します。複数台を同一 LAN へ接続できるよう、通常キッティングの前に各機器を DHCP へ切り替えます。

```text
(事前準備)
  |
  v
[スキャンガンで店舗コードと機器 EUI を登録]
  |
  v
[IP / DNS 情報を登録]
  |
  v
[証明書を作業 PC のローカル領域へ配置]
  |
  v
[UG65 の電源を投入]
  |
  v
[固定 IP 192.168.23.150 から DHCP へ切替]
  |
  v
[機器スキャンで DHCP 割当後の UG65 を検出]
  |
  v
[キッティングを実行]
  |
  v
(完了)
```
